# Tailperf Configuration
# Copy this file to .env and fill in your values

# Tailscale auth key (required for server and client modes)
# Get one from: https://login.tailscale.com/admin/settings/keys
TAILPERF_AUTHKEY=tskey-auth-xxxxx

# Optional: Custom hostname (default: tailperf-<pid>)
#TAILPERF_HOSTNAME=tailperf-server

# Optional: State directory (default: ~/.tailperf/<hostname>)
#TAILPERF_STATE_DIR=/var/lib/tailperf

# Optional: Filter for testing specific peers (default: tailperf)
#TAILPERF_FILTER=tailperf

# Optional: Test only this specific peer (exact match, takes precedence over filter)
#TAILPERF_TARGET=tailperf-server-1
